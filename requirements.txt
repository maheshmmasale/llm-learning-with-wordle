# Minimal stack: environment, search, evaluation, tests (milestones 1-4).
# Everything here installs on a plain laptop with no GPU. For model
# training and inference (milestones 5+), also install requirements-ml.txt.
PyYAML>=6.0.2,<7.0.0
pytest>=8.3.3,<9.0.0
pytest-cov>=5.0.0,<7.0.0
hypothesis>=6.115.0,<7.0.0
ruff>=0.7.1,<1.0.0
